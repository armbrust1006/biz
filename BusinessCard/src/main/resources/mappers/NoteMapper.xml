<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="global.scit.bizcard.dao.NoteDAO" >

<select parameterType="String" resultType="Note" id="noteList"> 
	select to_char(startdate, 'yyyy-MM-dd') as "start", to_char(enddate, 'yyyy-MM-dd') as "end", note as "title", color
	from cardnote_test where m_id=#{m_id}
</select>

<insert parameterType="Note" id="addNote">
	insert into CARDNOTE_TEST values(
	#{m_id}, #{cardnum}, #{start}, #{end}, sysdate, #{title}, #{color} 
	)
</insert>

<select parameterType="Note" resultType="int" id="getCard">
	select cardnum from cardnote_test where startdate = #{start} and note = #{title}
</select>


</mapper>