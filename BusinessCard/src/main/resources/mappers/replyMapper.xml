<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="global.scit.bizcard.dao.ReplyDAO">
	<select id="replyList" resultType="Reply">

		select reply_num,
		shared_cardnum,
		cardnum,
		m_id,
		to_char(inputdate,'YY-MM-DD') inputdate,
		reply
		from reply
		where
		shared_cardnum = {param1}
	</select>

	<insert id="replyInsert" parameterType="Reply">
		insert
		into reply(
		reply_num,
		shared_cardnum,
		cardnum,
		m_id,
		inputdate,
		reply
		)
		values (
		replynum_seq.nextval,
		#{shared_cardnum},
		#{cardnum},
		#{m_id},
		sysdate,
		#{reply})
	</insert>


	<!-- <update id="replyUpdate" parameterType="Reply"> </update> <delete id="replyDelete" 
		parameterType="Integer"> </delete> -->
</mapper>